import React from 'react';
import ReactDOM from 'react-dom/client';
import ToppingBar from './ToppingBar';
ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><ToppingBar /></React.StrictMode>);
