{
  "name": "ask-sja-topping",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8790",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "test": "vitest run src && node --test scripts/lib/corpus.test.mjs",
    "typecheck": "tsc --noEmit",
    "refresh:corpus": "node scripts/fetch_corpus.mjs"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "unpdf": "^1.8.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260916.1",
    "typescript": "^5.4.5",
    "vitest": "^3.2.7",
    "wrangler": "^4.73.0"
  }
}
